## PATH
export PATH=$HOME/bin:/usr/local/bin:$PATH
#export PATH="$PATH:$HOME/.rvm/bin:$GOPATH/bin:/usr/local/go/bin:$HOME/bin"

## GO STUFF
export GOPATH=$HOME/src/go
export CLICOLOR=1
export EDITOR='vim'

# Path to your oh-my-zsh installation.
export ZSH=/Users/pwagner/.oh-my-zsh

# Set name of the theme to load. Optionally, if you set this to "random"
# it'll load a random theme each time that oh-my-zsh is loaded.
# See https://github.com/robbyrussell/oh-my-zsh/wiki/Themes
ZSH_THEME="mh-pl"

# Set list of themes to load
# Setting this variable when ZSH_THEME=random
# cause zsh load theme from this variable instead of
# looking in ~/.oh-my-zsh/themes/
# An empty array have no effect
# ZSH_THEME_RANDOM_CANDIDATES=( "robbyrussell" "agnoster" )

# Uncomment the following line to use case-sensitive completion.
# CASE_SENSITIVE="true"

# Uncomment the following line to use hyphen-insensitive completion. Case
# sensitive completion must be off. _ and - will be interchangeable.
# HYPHEN_INSENSITIVE="true"

# Uncomment the following line to disable bi-weekly auto-update checks.
# DISABLE_AUTO_UPDATE="true"

# Uncomment the following line to change how often to auto-update (in days).
# export UPDATE_ZSH_DAYS=13

# Uncomment the following line to disable colors in ls.
DISABLE_LS_COLORS="false"

# Uncomment the following line to disable auto-setting terminal title.
# DISABLE_AUTO_TITLE="true"

# Uncomment the following line to enable command auto-correction.
# ENABLE_CORRECTION="true"

# Uncomment the following line to display red dots whilst waiting for completion.
# COMPLETION_WAITING_DOTS="true"

# Uncomment the following line if you want to disable marking untracked files
# under VCS as dirty. This makes repository status check for large repositories
# much, much faster.
# DISABLE_UNTRACKED_FILES_DIRTY="true"

# Uncomment the following line if you want to change the command execution time
# stamp shown in the history command output.
# The optional three formats: "mm/dd/yyyy"|"dd.mm.yyyy"|"yyyy-mm-dd"
# HIST_STAMPS="mm/dd/yyyy"

# Would you like to use another custom folder than $ZSH/custom?
# ZSH_CUSTOM=/path/to/new-custom-folder

# Which plugins would you like to load? (plugins can be found in ~/.oh-my-zsh/plugins/*)
# Custom plugins may be added to ~/.oh-my-zsh/custom/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
# Add wisely, as too many plugins slow down shell startup.
plugins=(git docker vagrant chef knife kubectl)

source $ZSH/oh-my-zsh.sh
source ~/.oh-my-zsh/custom/plugins/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh

# User configuration

# export MANPATH="/usr/local/man:$MANPATH"

# You may need to manually set your language environment
# export LANG=en_US.UTF-8


# Compilation flags
# export ARCHFLAGS="-arch x86_64"

## SSH STUFF
export SSH_KEY_PATH="~/.ssh/rsa_id"

# Set personal aliases, overriding those provided by oh-my-zsh libs,
# plugins, and themes. Aliases can be placed here, though oh-my-zsh
# users are encouraged to define aliases within the ZSH_CUSTOM folder.
# For a full list of active aliases, run `alias`.
#
# Example aliases
# alias zshconfig="mate ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"

#export LC_ALL=en_US.UTF-8

## thefuck
eval $(thefuck --alias shit)

## Uncomment the following line to enable command auto-correction.
## ENABLE_CORRECTION="true"
#
## Uncomment the following line to display red dots whilst waiting for completion.
#COMPLETION_WAITING_DOTS="true"
#
## Uncomment the following line if you want to change the command execution time
## stamp shown in the history command output.
## The optional three formats: "mm/dd/yyyy"|"dd.mm.yyyy"|"yyyy-mm-dd"
#HIST_STAMPS="dd/mm/yyyy"
#
#
#
##krun() {
##      kubectl get nodes | awk 'NR<2{next}{print $1}' | xargs -t -I % ssh % $1
##}
##

#
##################
## Aliases stuff #
##################

# ls aliases
alias ls="ls -l"

# RVM alaises
#
alias rgu="rvm gemset use"

# Kubectl aliases
#
alias kk="kubectl"
alias kkuc="kubectl config use-context"
alias kkgc="kubectl config get-contexts"

# Knife aliases
#
alias kb="knife block"

# Knife nodes aliases
alias knl="knife node list"
alias kns="knife node show"
alias kne="knife node edit"

# Knife data bags aliases
alias kdbl="knife data bag list"
alias kdbs="knife data bag show"
alias kdde="knife data bag edit"
alias kdbc="knife data bag create"
alias kdbe-lp="knife data bag --secret-file=~/.chef/encrypted_data_bag_secret edit"
alias kdbs-lp="knife data bag --secret-file=~/.chef/encrypted_data_bag_secret show -F json"
alias kdbc-lp="knife data bag --secret-file=~/.chef/encrypted_data_bag_secret create"
alias kdbe-ts="knife data bag --secret-file=~/.chef/encrypted_data_bag_secret_ts edit"
alias kdbs-ts="knife data bag --secret-file=~/.chef/encrypted_data_bag_secret_ts show -F json"
alias kdbc-ts="knife data bag --secret-file=~/.chef/encrypted_data_bag_secret_ts create"

# Knife cookbooks aliases
alias kcl="knife cookbook list"
alias kcs="knife cookbook show"
alias kcd="knife cookbook download"
alias kcu="knife cookbook upload"

# Knife roles aliases
alias krl="knife role list"
alias krs="knife role show"
alias kre="knife role edit"

#fpath=(~/.zsh/completion $fpath)
#autoload -Uz compinit && compinit -i
#
## Docker training first week in Nuglif
##
##BASEXRUN='docker run -v $HOME/:/root/ -v $HOME:$HOME -v /:/host -e LANG=$LANG --security-opt seccomp:unconfined -w /host/$PWD --rm -ti nud-docker-registry.lapresse.ca:5000/cli-toolkit:latest'
##alias xrun="$BASEXRUN "
##alias php="$BASEXRUN /usr/local/bin/php"
##alias phing="$BASEXRUN /usr/local/bin/phing"
##alias composer="$BASEXRUN /usr/local/bin/composer"
##alias genrb="$BASEXRUN /usr/bin/genrb"
##alias dbash="$BASEXRUN bash"
##alias dzsh="$BASEXRUN zsh"
#
## Git stuff
#
## Shotgun stuff
#
##export rvmsudo_secure_path=1
#
## Python stuff
#
##export VIRTUALENVWRAPPER_PYTHON=/usr/local/bin/python3

source <(kubectl completion zsh)
